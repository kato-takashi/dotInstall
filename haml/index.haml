- # encoding: utf-8
!!!
%html{:lang=> "ja"}
	%head
		%meta(charset="UTF-8")
		%body
			%p total is #{5 *10}
			%p= Time.now
			-x = 5
			%p= x
			- (1..10).each do |i|
				%p= i
				%p{:id => "item_#{i}"}#{i}}